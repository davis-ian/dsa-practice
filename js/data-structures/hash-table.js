
/**
 * Hash table = hash function + array
 *
 * Load factor = # of items / # of slots
 * Hash tables use an array for storage (count number of occupied slots)
 */

/**
 * Resizing: adding more slots to hash table
 * rule of thumb :
 * - create new array twice the size of original
 * - re insert all items into new array
 * - resize when load factor > 0.7
 */




